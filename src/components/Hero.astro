<div class="h-screen relative overflow-y-clip hero noise-background">
	<div>
		<img src="/hero.webp" alt="hero" id="heroForeground" class="lg:h-screen sm:h-3/4 md:h-5/6 h-2/3 drop-shadow-2xl preloadImg lg:top-0 bottom-0 sm:right-4 right-0 z-50 object-cover absolute" />
	</div>
	<div class="w-full h-screen object-cover absolute top-0 left-0 p-8 pt-16">
		<div style="opacity: 0;" class="p-8 heroText absolute z-10">
			<h1 class="sm:text-8xl text-6xl font-playfair font-bold tracking-tight text-white">
				Snoozier
			</h1>
			<h2 class="sm:text-2xl text-xl font-bold tracking-tight text-white">
				Beyond Ordinary Comfort
			</h2>

			<button class="sm:mt-12 mt-4 shop bg-white text-black px-4 py-2 rounded-full">
					Shop Now <span class="text-black text-xl font-semibold">→</span>
			</button>
		</div>
		<img src="/background.webp" alt="background" id="heroBackground" class="w-full h-full preloadImg rounded-[2.5rem] object-cover" />
	</div>
</div>

<script>
	import { showLoadingScreen } from '../scripts/animations/loadingScreen';
	import { delay } from '../scripts/utils';
	
	const shop = document.querySelector('.shop');
	
	shop?.addEventListener('click', async () => {
		showLoadingScreen();
		await delay(1000);
		window.location.href = '/shop';
	});
</script>